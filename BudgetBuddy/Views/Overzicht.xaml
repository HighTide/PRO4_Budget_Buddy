<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:BudgetBuddy"
             x:Class="BudgetBuddy.Views.Overzicht"
             Title="Totaal overzicht"
             BackgroundColor="{DynamicResource backgroundColor}">
    <ContentPage.BindingContext>
        <local:MainViewModel/>
    </ContentPage.BindingContext>

    <ListView ItemsSource="{Binding Products}"
              HasUnevenRows="True"
              Margin="0,40"
              ItemTapped="ListView_ItemTapped">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <StackLayout>
                        <Label Text="{Binding Title}" FontSize="Large" BackgroundColor="{Binding Background}" HorizontalTextAlignment="Center"/>
                        <Button Text="Details"
                                IsVisible="{Binding IsVisible}"/>
                        <Label Text ="Hello" IsVisible="{Binding IsVisible}"></Label>
                    </StackLayout>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>